# КОДЕР: current snapshot

## Смысл текущего состояния

Главное подтверждённое направление текущего чата — исследование исторических реализаций TERA и построение собственного решения не как косметического форка, а через понимание последовательных реализаций принципов.

Текущий практический объект — историческая версия `0.992`. Эксперимент направлен на запуск исходной исторической платформы без форковых настроек, изучение обычного поиска родительской сети и затем режима, в котором первая нода способна стать началом нового блокчейна.

## Статусы

### active

- исследование исходников исторической TERA `0.992`;
- runtime-аудит условий старта consensus (консенсуса), `CAN_START`, `LoadHistoryMode`, майнинга и сетевого bootstrap (первичного поиска узлов);
- экспериментальный запуск `0.992` на внешнем VDS `wbn.wbnetrus.ru`;
- журналирование этапов эксперимента;
- сравнение исторических реализаций TERA/TERA2 как инженерных конструкций.

### pending

- исполнение и проверка подготовленного Stage 04: остановка TERA2 на экспериментальном VDS, чистый запуск `0.992`, FindList (список первичных узлов) для `wbnetrus.ru` и `uk.wbnetrus.ru`, сбор runtime evidence;
- установление фактического поведения `0.992` при отсутствии исторической материнской сети;
- поиск штатного или минимально изменённого режима genesis/solo только после получения runtime evidence.

### parked

- построение собственной реализации нужных принципов до завершения сравнительного исследования исторических реализаций.

### legacy

- `packages/core/sledopyt-v01/`;
- старый контур `teraorigin_research` как provenance;
- абсолютные пути и старые задачи из Следопыт-пакета, пока они отдельно не подтверждены.

### unknown

- специальный демон публикационной цепочки WEB, который, по воспоминанию ОПЕРАТОРА, доставлял локально подготовленную копию сайта на burzh и erefia. В `wellbeing-entity-bootstrap` доказательства исходного кода, unit-файла, конфигурации или точного locator в проведённом assessment не найдены. Реконструкция по памяти запрещена;
- наличие иных current recovery КОДЕРА вне проверенного GitHub-контура и текущих предоставленных источников.

## Подтверждённые технические наблюдения текущего эксперимента

Для `0.992` по фактически просмотренному коду установлено: `LoadHistoryMode` переводит цикл в синхронизацию; `CAN_START` открывается после корректного обмена ping-временем с узлом; запуск miner process сам по себе не доказывает создание блоков. Локальный двухузловой эксперимент добился TCP-соединения и `CAN_START`, но это не дало подтверждённого построения новой цепочки.

Эти наблюдения являются исследовательским состоянием, а не изменением протокольного канона.

## Безопасный следующий шаг

Исполнить уже подготовленный внешний эксперимент Stage 04 и вернуть его архив результатов. Следующий вывод делать только по журналам и состоянию БД после запуска.

---
entity: KOD
document_type: snapshot
recovery_schema: v1.2
status: current-candidate-pending-coordinator
project_time: generated_without_trusted_project_time
