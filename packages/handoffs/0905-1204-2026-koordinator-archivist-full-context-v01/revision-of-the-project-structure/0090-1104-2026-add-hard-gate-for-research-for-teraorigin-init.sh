#!/data/data/com.termux/files/usr/bin/bash
set -euo pipefail

INIT_DIR="/storage/emulated/0/Documents/Blagopoluchie/00_INBOX/my-structure/chats-gpt-name/Research-for-teraOrigin/00_INIT_MINIMUM"

mkdir -p "$INIT_DIR"

cat > "$INIT_DIR/0004-1104-2026-hard-gate-init-regulation.md" <<'MD_EOF'
# Жёсткий регламент нулевого допуска для Research-for-teraOrigin

Дата: 2026-04-11
Статус: canonical
Контур: Research-for-teraOrigin
Назначение: запретить новой Сущности переход к следующим фазам до точного выполнения теста нулевого допуска

artifact_type: regulation
entity_scope: Research-for-teraOrigin
phase: bootstrap
repo_target: not_applicable
evidence_level: source_confirmed
handoff_to: Research-for-teraOrigin

## Главное правило

На нулевой фазе Сущность обязана выполнить только тот тест, который прямо задан оператором.

## Запрещено на нулевой фазе

- создавать любые дополнительные файлы, кроме прямо указанного;
- менять имя файла;
- менять путь файла;
- подменять тестовый документ “более умным” документом;
- синтезировать handoff, summary, integration, roadmap и другие побочные артефакты;
- вводить новые значения метаданных вне словаря пакета;
- переходить к следующей фазе без явного подтверждения оператора.

## Критерий допуска

Допуск к следующей фазе даётся только если:
- создан ровно тот файл, который запрошен;
- содержимое соответствует тесту;
- ответ выдан ровно одним shell-скриптом;
- не создано никаких побочных файлов.
MD_EOF

cat > "$INIT_DIR/0005-1104-2026-hard-gate-operator-prompt.md" <<'MD_EOF'
Ты проходишь нулевой допуск.

Сейчас нельзя переходить к исследованию репозитория, синтезу handoff, созданию summary или любым другим “умным” действиям.

Разрешено только одно:
точно выполнить тестовую задачу из файла `0003-1104-2026-first-capability-test.md`.

Жёсткие требования:
- выдать ровно один shell-скрипт;
- создать ровно один файл по указанному пути;
- не создавать других файлов;
- не менять имя файла;
- не менять путь;
- не придумывать дополнительные документы;
- не использовать значения метаданных, которых нет в словаре пакета;
- не переходить к следующей фазе без явного подтверждения оператора.

Если ты создашь любой другой файл вместо тестового, тест считается проваленным.

В ответ:
- одна короткая фраза о принятии правила;
- затем один bash-скрипт;
- ничего сверх этого.
MD_EOF

echo "Created:"
echo "$INIT_DIR/0004-1104-2026-hard-gate-init-regulation.md"
echo "$INIT_DIR/0005-1104-2026-hard-gate-operator-prompt.md"
